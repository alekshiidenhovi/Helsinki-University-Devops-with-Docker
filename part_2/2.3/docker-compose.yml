version: '3.5'

services:

  frontend:
    image: frontend
    container_name: front
    ports: 
      - 5000:5000

  backend:
    image: backend
    container_name: back
    ports: 
      - 8080:8080

# The images have to be first built from their respective Dockerfiles from part 1

# Command: dc up